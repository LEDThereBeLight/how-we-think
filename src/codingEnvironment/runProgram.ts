// Write utilities to create the AST from the graph
// sources / targets
// create an executable JS block for each type of node
// substitute in parameters from sources
// evaluate

// find a way to determine the reduction step (line number)
// display heap (inline if possible), call stack
